spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USERNAME}
spring.datasource.password=${DATASOURCE_PASSWORD}

lastwin.jwt.secret=${LASTWIN_JWT_SECRET}
lastwin.jwt.expiration=${LASTWIN_JWT_EXPIRATION}

lastwin.mail.user=${LASTWIN_MAIL_USER}
lastwin.mail.password=${LASTWIN_MAIL_PASSWORD}
lastwin.mail.host=${LASTWIN_MAIL_HOST}
lastwin.mail.port=${LASTWIN_MAIL_PORT}

lastwin.baseURL=${LASTWIN_BASEURL}